import 'dart:io';

void main() {
  stdout.write("Enter the Number :");
  int num = int.parse(stdin.readLineSync()!);
  for (int i = 1; i <= 10; i++) {
    print("your table $num x $i = ${num * i}");
  }
}
